{
  "name": "overline-community-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:test": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build:test && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
